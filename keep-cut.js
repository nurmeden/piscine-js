function cutFirst(s) {
    newstr = s.replace(s[0],'')
    newstr = s.replace(s[0],'')
    return newstr
}

function cutLast(s) {
    newstr = s.replace(s[s.length-1],'')
    newstr = s.replace(s[s.length-1],'')
    return s
}
